export enum OrderHistory {
  Pending = "Pending",
  Rejected = "Rejected",
  Accepted = "Accepted",
  Cancelled = "Cancelled",
  Timeout = "Timeout",
  CustomerCancelled = "Customer Cancelled",
}

export enum PaymentMethods {
  Credit = "Credit",
  Cash = "Cash",
}
